- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:
    ~/.npmrc:
    ~/.zshrc:
      force: true
    ~/.zshrc_aliases:

- snap:
    - code
    - dbeaver-ce
    - node

- shell:
    - [git submodule update --init --recursive, Installing submodules]
