- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:
    ~/.zshrc:
      force: true

- snap:
    - code

- shell:
    - [git submodule update --init --recursive, Installing submodules]
