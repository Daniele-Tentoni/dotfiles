- defaults:
    link:
      create: true
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:
    ~/.zshrc:
      force: true

- shell:
    - [git submodule update --init --recursive, Installing submodules]
